<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Process ID Display</title>
    <style>
      body {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
        background: #f5f7fa;
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        color: #2c3e50;
      }

      .process-container {
        background: white;
        padding: 2rem 3rem;
        border-radius: 12px;
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
        text-align: center;
        max-width: 400px;
        width: 90%;
        animation: fadeInScale 0.6s ease forwards;
      }

      .process-container h2 {
        font-size: 1.75rem;
        margin-bottom: 1rem;
        letter-spacing: 1.1px;
      }

      .process-id {
        font-size: 2.5rem;
        font-weight: 700;
        color: #2980b9;
        background: #d6eaff;
        padding: 0.5rem 1rem;
        border-radius: 8px;
        user-select: all;
        box-shadow: 0 4px 12px rgba(41, 128, 185, 0.25);
        display: inline-block;
        transition: background 0.3s ease;
        cursor: text;
      }

      .process-id:hover {
        background: #a9d0ff;
      }

      @keyframes fadeInScale {
        from {
          opacity: 0;
          transform: scale(0.85);
        }
        to {
          opacity: 1;
          transform: scale(1);
        }
      }

      .error-msg {
        font-size: 1.5rem;
        color: #e74c3c;
        font-weight: 600;
        text-align: center;
      }
    </style>
  </head>
  <body>
    <% if (pid && pid !== undefined) { %>
    <div class="process-container">
      <h2>Your Process ID</h2>
      <div class="process-id" tabindex="0" title="Click to select">
        <%= pid %>
      </div>
    </div>
    <% } else { %>
    <div class="error-msg">Registration Failed. Please try again.</div>
    <% } %>
  </body>
  <script>
    document.addEventListener("DOMContentLoaded", () => {
      const pidEl = document.querySelector(".process-id");
      if (pidEl) {
        pidEl.addEventListener("click", () => {
          navigator.clipboard.writeText(pidEl.innerText).then(() => {
            alert("Process ID copied to clipboard!");
          });
        });
      }
    });
  </script>
</html>
