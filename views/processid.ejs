<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Process ID Display</title>
  <style>
    /* ===== Reset & Base ===== */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif;
    }

    body {
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      background: url('/images/upload-bg-image.png') no-repeat center center/cover;
      padding: 1rem;
      color: #2c3e50;
    }

    /* ===== Card ===== */
    .card {
      background: #fff;
      padding: 2rem;
      border-radius: 16px;
      box-shadow: 0px 6px 20px rgba(0, 0, 0, 0.12);
      max-width: 500px;
      width: 100%;
      animation: fadeInScale 0.6s ease forwards;
    }

    .card h2 {
      font-size: 1.6rem;
      margin-bottom: 1.2rem;
      text-align: center;
      color: #222;
    }

    /* ===== Info Rows ===== */
    .info-row {
      margin-bottom: 1.2rem;
    }

    .info-row h3 {
      font-size: 1rem;
      font-weight: 600;
      margin-bottom: 0.5rem;
      color: #444;
    }

    .info-value {
      font-size: 1.1rem;
      font-weight: 600;
      color: #2980b9;
      background: #f0f8ff;
      padding: 0.6rem 1rem;
      border-radius: 10px;
      display: inline-block;
      width: 100%;
      text-align: center;
      box-shadow: 0 4px 10px rgba(41, 128, 185, 0.15);
      transition: transform 0.2s, background 0.3s;
      cursor: pointer;
      user-select: all;
      user-select: none;
    }

    .info-value:hover {
      background: #d6eaff;
      transform: scale(1.03);
    }

    /* ===== Admission Status Colors ===== */
    .status-approved {
      color: #27ae60;
      background: #e6f9ee;
      box-shadow: 0 4px 10px rgba(39, 174, 96, 0.2);
    }

    .status-rejected {
      color: #e74c3c;
      background: #fdecea;
      box-shadow: 0 4px 10px rgba(231, 76, 60, 0.2);
    }

    .status-pending {
      color: #f39c12;
      background: #fff6e5;
      box-shadow: 0 4px 10px rgba(243, 156, 18, 0.2);
    }

    /* ===== Contact ===== */
    .contact {
      text-align: center;
      margin-top: 1.5rem;
      font-size: 1rem;
    }

    .contact a {
      color: #007bff;
      text-decoration: none;
      font-weight: 600;
    }

    .contact a:hover {
      text-decoration: underline;
    }

    /* ===== Error Message ===== */
    .error-msg {
      background: #fff;
      padding: 1.5rem;
      border-radius: 12px;
      font-size: 1.2rem;
      color: #e74c3c;
      font-weight: 600;
      text-align: center;
      box-shadow: 0px 6px 18px rgba(0, 0, 0, 0.08);
    }

    /* ===== Popup (Copied) ===== */
    .popup {
      position: fixed;
      bottom: 20px;
      left: 50%;
      transform: translateX(-50%) translateY(100px);
      background: #2ecc71;
      color: #fff;
      padding: 0.8rem 1.2rem;
      border-radius: 8px;
      font-size: 1rem;
      font-weight: 600;
      opacity: 0;
      pointer-events: none;
      transition: opacity 0.3s ease, transform 0.3s ease;
      box-shadow: 0 6px 18px rgba(0, 0, 0, 0.15);
    }

    .popup.show {
      opacity: 1;
      transform: translateX(-50%) translateY(0);
    }

    @keyframes fadeInScale {
      from {
        opacity: 0;
        transform: scale(0.9);
      }
      to {
        opacity: 1;
        transform: scale(1);
      }
    }

    /* ===== Responsive ===== */
    @media (max-width: 480px) {
      .card {
        padding: 1.5rem;
      }

      .info-value {
        font-size: 1rem;
      }

      .card h2 {
        font-size: 1.3rem;
      }
    }
  </style>
</head>
<body>
  <% if (data && data !== undefined) { %>
    <div class="card">
      <h2>Admission Details</h2>

      <div class="info-row">
        <h3>Process ID</h3>
        <div class="info-value" title="Click to copy">
          <%= data.processId %>
        </div>
      </div>

      <div class="info-row">
        <h3>Email ID</h3>
        <div class="info-value" title="Click to copy">
          <%= data.email %>
        </div>
      </div>

      <div class="info-row">
        <h3>Father's Mobile Number</h3>
        <div class="info-value" title="Click to copy">
          <%= data.mobNumber %>
        </div>
      </div>

      <div class="info-row">
        <h3>Admission Status</h3>
        <div class="info-value <% if(data.admissionStatus === 'Approved'){ %>status-approved<% } else if(data.admissionStatus === 'Rejected'){ %>status-rejected<% } else { %>status-pending<% } %>">
          <% if(data.admissionStatus == 'PACR'){%>Pending At Counselor<%} else if(data.admissionStatus == 'DVRL'){%>Document Verification<%}else{%>Admission Done<%}%>
        </div>
      </div>

      <div class="contact">
        For any query, <a href="/contact">contact us</a>
      </div>
    </div>
  <% } else { %>
    <div class="error-msg">Registration Failed. Please try again.</div>
  <% } %>

  <!-- Popup -->
  <div class="popup" id="copyPopup">Copied!</div>

  <script>
    document.addEventListener("DOMContentLoaded", () => {
      const copyableEls = document.querySelectorAll(".info-value");
      const popup = document.getElementById("copyPopup");

      copyableEls.forEach(el => {
        el.addEventListener("click", () => {
          navigator.clipboard.writeText(el.innerText).then(() => {
            // Show popup
            popup.classList.add("show");
            setTimeout(() => {
              popup.classList.remove("show");
            }, 1500);
          });
        });
      });
    });
  </script>
</body>
</html>
