<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/style/gallery.css" />
  <script src="/script/gallery.js" defer></script>
  <title>Gallery</title>
</head>

<body>
  <!-- NAV-->
  <%- include('index.ejs') %>

    <!-- HEADING -->
    <section class="gallery">
      <div class="gallery-overlay"></div>
      <div class="gallery-inner">
        <h1>Gallery</h1>
        <p class="subtitle">
          Our gallery shows learning, creativity, discipline, and values that
          shape our community.
        </p>
      </div>
    </section>

    <!-- GALLERY SECTION -->
    <main>
      <section class="gallery-heading container">
        <div class="gallery-options">
          <a href="#" class="active" data-filter="all">All</a>
          <a href="#" data-filter="date">Arranged by Date</a>
          <a href="#" data-filter="title">Arranged by Event Name</a>
        </div>

        <!-- ALL IMAGES (Flat Grid) -->
        <section class="gallery-section all">
          <% // Reverse the array so first image shows last 
            const allImages=images.slice().reverse();
            allImages.forEach(img=> {%>
            <div class="gallery-image-card">
              <img src="<%= img.img %>" alt="<%= img.title %>" />
              <div class="gallery-card-inner-text">
                <h3><%= img.title %></h3>
              </div>
            </div>
            <% }); %>
        </section>

        <!-- GROUPED BY DATE -->
        <section class="gallery-section bydate" style="display: none;">
          <% if (images && images.length> 0) {
            // Sort images by EventDate descending (recent first)
            const sortedByDate = images.slice().sort((a,b) => b.EventDate.localeCompare(a.EventDate));
            let grouped = {};
            sortedByDate.forEach(img => {
            grouped[img.EventDate] = grouped[img.EventDate] || [];
            grouped[img.EventDate].push(img);
            });

            // Sort dates descending
            const dates = Object.keys(grouped).sort((a,b) => b.localeCompare(a));

            dates.forEach(date => {
            const imgs = grouped[date];
            const d = date.split('-');
            %>
            <div class="gallery-card-group">
              <h2 class="group-heading">Gallery | <%= `${d[2]}-${d[1]}-${d[0]}` %>
              </h2>
              <div class="group-images">
                <% imgs.reverse().forEach(img=> { %>
                  <div class="gallery-image-card">
                    <img src="<%= img.img %>" alt="<%= img.title %>" />
                    <div class="gallery-card-inner-text">
                      <h3><%= img.title %></h3>
                    </div>
                  </div>
                  <% }); %>
              </div>
            </div>
            <% }); } %>
        </section>

        <!-- GROUPED BY EVENT NAME -->
        <section class="gallery-section bytitle" style="display: none;">
          <% if (images && images.length> 0) {
            const sortedByTitle = images.slice().sort((a,b) => a.title.localeCompare(b.title));
            let groupedTitle = {};
            sortedByTitle.forEach(img => {
            groupedTitle[img.title] = groupedTitle[img.title] || [];
            groupedTitle[img.title].push(img);
            });

            const titles = Object.keys(groupedTitle).sort(); // alphabetically
            titles.forEach(title => {
            const imgs = groupedTitle[title];
            %>
            <div class="gallery-card-group">
              <h2 class="group-heading">Gallery | <%= title %>
              </h2>
              <div class="group-images">
                <% imgs.forEach(img=> { %>
                  <div class="gallery-image-card">
                    <img src="<%= img.img %>" alt="<%= img.title %>" />
                    <div class="gallery-card-inner-text">
                      <h3><%= img.title %></h3>
                    </div>
                  </div>
                  <% }); %>
              </div>
            </div>
            <% }); } %>
        </section>

        <!-- IMAGE POPUP -->
        <div class="popup" id="popup">
          <div class="popup-container">
            <div class="close-btn">
              <img src="/images/close-icon.svg" alt="close" />
            </div>
            <div class="popup-content">
              <img class="popup-img" src="" alt="popup image" />
            </div>
          </div>
        </div>
      </section>
    </main>

    <!-- FOOTER -->
    <footer>
      <div class="footer-container">
        <div class="about-us">
          <h3>About Us</h3>
          <div>
            <p>
              St. Columbus Public School is a private English Medium School
              established for providing quality education to students up to High
              School level with a focus on values, discipline, and all-round
              development.
            </p>
          </div>
        </div>
        <div class="quick-links">
          <h3>Quick Links</h3>
          <div>
            <a href="/">Home</a>
            <a href="/about">About Us</a>
            <a href="/admission">Admission</a>
            <a href="/gallery">Gallery</a>
            <a href="/contact">Contact Us</a>
          </div>
        </div>

        <div class="follow-us">
          <h3>Follow Us</h3>
          <div>
            <a href="#"><i class="fab fa-youtube"></i> YouTube</a>
            <a href="#"><i class="fab fa-facebook"></i> Facebook</a>
            <a href="#"><i class="fab fa-instagram"></i> Instagram</a>
            <a href="#"><i class="fab fa-x-twitter"></i> X</a>
          </div>
        </div>

        <div class="contact-us">
          <h3>Contact Us</h3>
          <div>
            <p>
              <i class="fas fa-map-marker-alt"></i>
              <a
                href="https://maps.google.com/?q=St.+Columbus+Public+School,+Ekdara,+Jhajha,+Jamui+811308+(Bihar),+India">
                St. Columbus Public School, Ekdara, &nbsp; &nbsp;&nbsp; Jhajha,
                Jamui 811308 (Bihar), India</a>
            </p>
            <p>
              <i class="fas fa-phone"></i>
              <a href="tel:+919812345678">+91-9876543210</a>,
              <a href="tel:+919812345678">+91-1122334455</a>
            </p>
            <p>
              <i class="fas fa-envelope"></i>
              <a href="https://mail.google.com/mail/?view=cm&fs=1&to=sudhanshu95084145@gmail.com&su=Hello&body=This%20is%20a%20default%20message"
                target="_blank">info@gvpschool.edu.in</a>
            </p>
          </div>
        </div>
      </div>
      <p class="copy-right">
        <i class="far fa-copyright"></i> 2025 St. Columbus Public School. All
        rights reserved. | Developed by
        <a href="https://www.grabityservices.com" target="_blank">GrabityServices</a>
        <a id="upArrow" style="margin-left: 20px;color: aliceblue;" href="#upp">&uparrow;</a>
      </p>
    </footer>


</body>

</html>